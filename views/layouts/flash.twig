{# 
  Display the flash if it's there
 #}

{% if App.user.hasFlash('error') %}
<br />
<div class="panel panel-danger">
	<div class="panel-heading">
    <h3 class="panel-title">Error</h3>
  </div>
  <div class="panel-body">
    {{ this.t(App.user.getFlash('error')) }}
  </div>
</div>
{% endif %}
		